# Space Explorer: implementation progress

Updated 2026-09-08. The single source for where the work stands, so the other documents state status once and link here ([decision 0011](decisions/0011-requirements-single-source.md) applies the same rule to platform policy).

It opens with the record of every step that has passed, then tracks the [development plan](development-plan.md) exactly: the same milestones in the same order, the plan's M0a exit criteria in the order the plan states them, the solar-system iterations the plan's M0b row names, the plan's twelve verification checks under their own names, and the plan's seven open production decisions. Nothing is added to the plan's process and nothing is dropped from it; rows the plan does not contain appear only in the section labelled as such.

A step is recorded as passed only with the evidence that closed it, never on the strength of having been written. Status words mean one thing throughout: **Done** is implemented and checked by the evidence named; **Partial** states exactly which part holds; **Not started** means no code or record exists. "Passes on Linux and Windows" always means both were run, never that one was run and the other inferred.

Local results are from a Debian 13 x86_64 workstation. No performance target has been measured.

## Completed steps

Every step that has passed, in the order it passed, with the evidence that closed it and the commit that carries it. Steps 1 to 15 are from 2026-09-07; each later step carries its own date.

| # | Step | Passed on the evidence of | Commit |
| --- | --- | --- | --- |
| 1 | Concept and owner requirements captured; `requirements.md` declared the single source for platform policy and provenance ([decision 0011](decisions/0011-requirements-single-source.md)) | Concept of record fixed; platform restatements elsewhere reduced to one sentence and a link | `3faa59e`, `0a98fb0` |
| 2 | Technology assessment and stack selection: Godot 4 .NET with C#, engine-independent core, SQLite | Checked against requirements R1 to R5, with a five-engine alternatives table sourced from vendor pages | `0a98fb0`, `7d544e1` |
| 3 | Design review, first round: sixteen findings raised and cleared, decisions 0001 to 0015 accepted | Every internal link and anchor resolves; cited external URLs respond; worked valuation examples check out arithmetically | `194e0de` |
| 4 | Platform confirmed and toolchain pinned: Godot 4.7.2 .NET, .NET SDK 10.0.400, `net10.0`, central package versions ([decision 0016](decisions/0016-platform-confirmed-and-toolchain-pinned.md)) | Assessment showed Godot alone is free to use with a first-class Linux editor, native Linux and Windows exports, and official C# | `45d0bef` |
| 5 | Solution scaffolded by assembly: seven projects, nullable reference types, implicit usings, warnings as errors, code-style enforcement ([decision 0009](decisions/0009-solution-layout.md)) | `dotnet build` succeeds with zero warnings across all seven projects | `45d0bef` |
| 6 | Structural test baseline | Three tests pass: Core references no Godot assembly, Persistence references no Godot assembly, and the native SQLite library loads and reports a version | `45d0bef` |
| 7 | Continuous integration on both operating systems | GitHub Actions runs restore, build, and test on `ubuntu-latest` and `windows-latest` for every push to `main` and every pull request | `45d0bef` |
| 8 | Godot project and export presets: Compatibility renderer, root `Main` scene, x86_64 presets for Linux and Windows Desktop | Windows preset cross-exports from Linux without Wine | `45d0bef` |
| 9 | Linux exported-build smoke test | `smoke.sh` exports and the packaged build prints `SMOKE OK` with Godot 4.7.2, .NET 10.0.11, and SQLite 3.53.3; run manually on the reference workstation | `45d0bef` |
| 10 | Design review, second round: findings 17 to 19 cleared. Region storage constants corrected and the extent cap set to 2,048 m ([decision 0017](decisions/0017-region-extent-cap-and-storage-derivation.md)); data root, package layout, region encoding, and save integrity specified ([decision 0018](decisions/0018-data-root-region-encoding-and-save-integrity.md)) | The over-determined region constants were caught by deriving bytes from the constants rather than restating the estimate | `7d544e1`, `f774a1a`, `dbe079e` |
| 11 | M0a step one: deterministic random foundation frozen as generator version 1, whose mix construction was superseded by step 13 before anything was generated under it. `Pcg32`, `Mix64`, `StreamPath`, `RandomStream`, `GeneratorVersion` ([decision 0019](decisions/0019-generator-version-1-frozen.md)) | 41 Core tests pass; both external anchors hold, the PCG32 reference demo for seed 42 and stream 54 and the SplitMix64 first output for seed 0; identical results on `ubuntu-latest` and `windows-latest`; mutation-checked on three frozen constants; `RS0030` re-verified on a `System.Random` use in Core | `f774a1a` (source), `9ab5838`, `1bbd8f9` (record) |
| 12 | M0a step two: canonical encoding format 1 and the SHA-256 content hash frozen. `CanonicalWriter`, `ContentHash`, `PackId` ([decision 0020](decisions/0020-canonical-encoding-and-content-hash.md)) | 79 Core tests pass, 38 of them new; the published SHA-256 vectors for the empty input and for `abc` hold; the recorded 47-byte vector follows by hand from the frozen rules and its hash was computed outside this repository, not by the code under test; mutation-checked on five rules, big-endian fixed-width values, a dropped length prefix, a dropped domain, a pack identifier taken from the tail of the hash, and a dropped zig-zag step, each of which turns the suite red; identical results on `ubuntu-latest` and `windows-latest` | `a103471` (source), `8b0c291` (record) |
| 13 | M0a step three: stream derivation moved to SHA-256 over a canonical record, as generator version 2. `Mix64` deleted ([decision 0021](decisions/0021-sha256-stream-derivation.md), clearing [review finding 20](review.md#20-two-hashing-primitives-where-one-would-do)) | 69 Core tests pass on `ubuntu-latest` and `windows-latest`; the three derivation vectors are new and their digests were computed by an implementation outside this repository from the preimage a test documents; mutation-checked on four rules, swapped halves, a big-endian digest read, a changed domain label, and an increment not forced odd, each of which turns the suite red | `acdc063` (source) |
| 14 | Implementation-review findings 21 and 23 to 27 cleared: the P0 exit record precedes the first frozen content format ([decision 0022](decisions/0022-p0-gates-the-first-frozen-content-format.md)); `Pcg32` is a sealed class ([decision 0023](decisions/0023-pcg32-is-a-sealed-class.md)); six redundant tests, the generated document index, the Benchmarks project, and the code-style flag removed, and the `Shared/` comments reduced to pointers ([decision 0024](decisions/0024-tests-comments-index-and-scaffolding-trimmed.md)) | Build with zero warnings; 69 Core and 2 Persistence tests pass on the Debian 13 workstation; docs check clean; `Shared/` comment lines 326 to 142 with 307 code lines unchanged; the same suite passes on `ubuntu-latest` and `windows-latest` in continuous integration | `7bcbbbc` |
| 15 | M0a step four: exact references, category registry revision 1, the definition, template, vocabulary, specification, and manifest records, the set generator, the publish protocol with `index.db`, and the CLI vertical slice ([decisions 0033](decisions/0033-generated-definitions-have-one-revision.md) to [0042](decisions/0042-category-registry-revision-1-first-content-records-generator-and-publish-protocol.md)) | Build with zero warnings; 161 Core and 11 Persistence tests pass on the Debian 13 workstation; mutation-checked on four rules, a table keying duplicates by exact reference, a generator accepting duplicate content, a loader skipping the hash check, and a publisher accepting a differing manifest, each of which turns the suite red; `generate-set` publishes the `basic` vocabulary at seed 42 as 36 definitions in 37 content-addressed records, `inspect` and `validate` reload them without generation, a second run is a no-op, and a corrupted record is refused; the full suite passes on `ubuntu-latest` and `windows-latest` in continuous integration for `b7e2df9` | `9735a88` (exact references), `20c18f7` (record and slice), `b7e2df9` (Windows pooling fix) |
| 16 | 2026-09-08. The first two parts of [decision 0047](decisions/0047-usable-storage-and-structure-generation-then-solar-system-iterations.md) step one: the two-process determinism comparison over `generate-set`, and a loader that accepts every category-registry revision the build supports and refuses the rest by name ([decision 0035](decisions/0035-category-registry-record-and-generator-revision-identifiers.md)) | Build with zero warnings; 179 tests pass on the Debian 13 workstation, 161 in Core, 15 in Persistence, and 3 in the new `SpaceExplorer.Cli.Tests`, which runs `generate-set` twice as separate operating-system processes into separate data roots and compares every published record byte for byte, then validates the pack from a third process; a pack published under an expanded registry revision 2 loads beside a revision 1 pack from one data root; mutation-checked on two rules, a loader that skips the registry-hash check and a supported-revision lookup that ignores the revision asked for, each of which turns the suite red | `PENDING` |

Two notes on the record above. An initial TypeScript scaffold was created and then replaced when the Linux-first Godot C# stack was documented, so it is not a passed step. The source of step 11 landed in `f774a1a`, a commit whose message describes step 10, because a parallel session staged the whole tree while those files were being written; the content is the reviewed content, but the message does not describe it.

## Milestones

| Milestone | Status |
| --- | --- |
| P0: greybox expedition prototype | **In progress.** Built at `prototypes/p0-expedition/`: one hard-coded region, three sites, four artifacts, a two-slot cargo limit with a swap prompt, a return to the home anchor, a sale screen, and a destination-choice screen, exactly decision 0015's scope ([decision 0025](decisions/0025-p0-assessment-location-scope-and-players.md)). Builds clean, imports, and passes its smoke check both from source and from a headless Linux export on the Debian 13 workstation on 2026-09-07. Not done: the exit record itself, which needs three to five real players ([decision 0015](decisions/0015-greybox-prototype.md)) and is not something a build or a smoke check can stand in for. It no longer blocks M0a criterion 1: the owner accepted the named format-compatibility cost in [decision 0030](decisions/0030-m0a-criterion-1-proceeds-before-the-p0-exit-record.md). |
| M0a: primitive-set foundation | **In progress.** Of the plan's sixteen exit criteria below, three are done, ten are partial, and three are not started. Decision 0031 defines the complete primitive/storage boundary and [decision 0042](decisions/0042-category-registry-revision-1-first-content-records-generator-and-publish-protocol.md) implements its first records: registry revision 1, the definition, template, vocabulary, specification, and manifest formats, a generator that publishes sets from an authored vocabulary, and a CLI that reloads and validates them. Review findings [31 to 36](review.md#findings-raised-by-the-primitive-storage-inspection), raised against that boundary, and [37 to 41](review.md#findings-raised-by-the-planetary-model-sufficiency-check), raised against the planetary model it must carry, are cleared by decisions 0033 to 0041, so the definition records can be written. The frozen random foundation is not itself one of the plan's M0a criteria; it appears under Determinism in the verification table. The canonical encoding and content hash appear in criterion 5. |
| M0b: world generation and save prototype | **Not started.** It opens with the solar-system iterations of [decision 0047](decisions/0047-usable-storage-and-structure-generation-then-solar-system-iterations.md), none of which has run; see [Solar-system iterations](#solar-system-iterations). This includes the deterministic observer-centred celestial solution and generated epoch/orbit/rotation/atmosphere data required by [decision 0032](decisions/0032-astronomically-consistent-surface-sky.md), the derived system description command with one fixed-seed description vector ([decision 0045](decisions/0045-close-views-only-in-the-surface-sky-and-a-derived-system-description.md)), and validation of the per-tier planet and life rules of [decision 0044](decisions/0044-tiered-system-composition-one-star-and-explorability-by-validation.md). Decisions [0040](decisions/0040-reference-index-and-deletion-sweep.md) and [0041](decisions/0041-planet-fields-tangent-regions-minimum-radius-and-far-field.md) fix the reference index, the cube-sphere planet fields, the tangent-plane region mapping, the 524,288 m minimum landable radius, and the derived far field that the first world package needs. |
| M1: solo playable expedition | **Not started.** This includes integrating the M0b celestial solution with surface sky, atmosphere, and directional lighting, and the arrival screen showing the system description ([decision 0045](decisions/0045-close-views-only-in-the-surface-sky-and-a-derived-system-description.md)). |
| M2: connected core release | **Not started.** |
| M3: expansion experiments | **Not started.** |

## M0a exit criteria

The plan's M0a row, decomposed in its own order.

| # | Criterion | Status |
| --- | --- | --- |
| 1 | Core and CLI generate random, constraint-valid primitive sets from authored templates | **Partial.** `SetGenerator` draws definitions from `content/templates/basic/vocabulary.json` under registry revision 1 on per-instance streams, rejects duplicate content within the retry budget, and `generate-set` publishes the result ([decision 0042](decisions/0042-category-registry-revision-1-first-content-records-generator-and-publish-protocol.md)). Held: range and reference validity, deterministic reproduction of a recorded manifest hash. Not held: connector-tag compatibility is declared but not enforced until composition at M0b, only one vocabulary exists, and the content gate has not judged the output.
| 2 | Allocate IDs | **Partial.** Generated packs allocate 1..n in generation order with zero reserved, and the pack identifier is the leading 16 bytes of the specification hash ([decision 0006](decisions/0006-pack-identity-and-allocation.md), [decision 0020](decisions/0020-canonical-encoding-and-content-hash.md)); a manifest listing IDs out of order is refused. Not held: the authored pack's persisted allocation ledger; template IDs are checked ascending and distinct in the vocabulary only. |
| 3 | Validate, store, reload, and query sets without rerolling | **Done.** `SetPublisher` writes every record to `packs/<pack-id>/<hash>.bin` through a flushed, re-read, verified temporary file and commits `index.db` last; `SetLoader` reloads a pack from the index without generating, verifies each record against its name before decoding, refuses another registry revision, and checks the definitions against the manifest and each other; `PrimitiveSet.TryFind` and `Contains` answer identity and exact-reference queries; `inspect` and `validate` do this from the CLI ([decision 0042](decisions/0042-category-registry-revision-1-first-content-records-generator-and-publish-protocol.md)). `CanonicalReader` round-trips every field decision 0020 froze and is mutation-checked on four rules. Every decoder rejects trailing bytes and non-canonical encodings by re-encoding. |
| 4 | Specify cross-package set operations without implementing them | **Done.** `PrimitiveId`, `PrimitiveRevisionRef`, `Handle`, `ReferenceTable`, and `ICrossPackageSetOperations` exist under `src/SpaceExplorer.Core/Registry/` ([decision 0029](decisions/0029-primitive-identity-handle-and-cross-package-set-operations-contract.md)). Since decision 0031 the table resolves handles to exact references of `PrimitiveId` plus content hash and the operations take sets of exact references; the table rejects a repeated `PrimitiveId` under [decision 0033](decisions/0033-generated-definitions-have-one-revision.md), and a structural test asserts nothing implements the operations. Mutation-checked on three rules: accepting a repeated identity, keying duplicates by exact reference so two revisions of one ID pass, and an off-by-one bounds check, each of which turns the suite red. 134 Core tests pass on the Debian 13 workstation. |
| 5 | Verify canonical hashes on Linux and Windows | **Partial.** The canonical encoding and the SHA-256 content hash exist and are frozen as format 1 ([decision 0020](decisions/0020-canonical-encoding-and-content-hash.md)), with a recorded vector and a derived pack identifier that the suite checks. That suite passed on `ubuntu-latest` and on `windows-latest` in continuous integration for `8b0c291` on 2026-09-07, 79 Core tests on each, so the recorded vector, its content hash, and the pack identifier derived from it are byte-identical on both operating systems. The registry, vocabulary, specification, manifest, and definition records now exist ([decision 0042](decisions/0042-category-registry-revision-1-first-content-records-generator-and-publish-protocol.md)), and their recorded hashes hold on both operating systems: the 161 Core tests, which pin the registry hash and the fixture manifest hash, passed on `ubuntu-latest` and `windows-latest` in continuous integration for `20c18f7` on 2026-09-07. That run still failed overall, because 7 Persistence tests could not delete their temporary data root on Windows while a pooled SQLite connection held `index.db` open; pooling is now disabled and the re-run for `b7e2df9` passed the full suite on both operating systems. |
| 6 | Missing and corrupt dependencies fail explicitly; data-only package bounds and material/texture/mesh dependency hashes are verified | **Partial.** A missing record, a record that does not hash to its name, a manifest pinning another registry revision, an unknown pack, and a definition referencing a later or foreign definition each fail with a named exception ([decision 0042](decisions/0042-category-registry-revision-1-first-content-records-generator-and-publish-protocol.md)); every list length is bounded before allocation. Not held: no mesh or texture asset exists yet, so asset hashes are untested. |
| 7 | ID and revision boundaries and collisions | **Partial.** Zero is refused as a category, template, and local ID; a repeated `PrimitiveId` in a reference table and two revisions of one ID are refused; a second differing manifest under one `pack_id` is refused before anything is written ([decision 0039](decisions/0039-one-manifest-per-pack-identifier.md)), mutation-checked. Not held: `uint.MaxValue` and overflow rejection, and mixed-pack lookup, which no consumer needs yet. |
| 8 | Bounded failures | **Partial.** A template whose variation is exhausted fails after its retry budget with an explicit message, and a reference no earlier definition supplies fails at once; nothing is published in either case ([decision 0042](decisions/0042-category-registry-revision-1-first-content-records-generator-and-publish-protocol.md)). Not held: world-level retry and fallback, which is M0b. |
| 9 | Interrupted publication | **Partial.** Records are written to temporary files, flushed, re-read, verified, and moved into place without overwriting different content, the index is committed last, and a leftover temporary file is removed by the next publication, which then completes ([decision 0042](decisions/0042-category-registry-revision-1-first-content-records-generator-and-publish-protocol.md)). Not held: crash injection between the file moves and the index commit. |
| 10 | Expansion compatibility | **Partial.** `SetLoader` takes the revisions the build supports rather than one registry, selects the revision a manifest pins, and refuses an unsupported revision or a differing hash for a supported revision with a compatibility error naming it, never a substitution ([decision 0035](decisions/0035-category-registry-record-and-generator-revision-identifiers.md)); a pack published under a registry revision 2 that expands revision 1 loads beside a revision 1 pack from one data root, mutation-checked on the hash check and the revision lookup. Not held: the shipped build supports revision 1 only, so the second revision exists in the test alone; no world, save, or revised template pack exists to preserve across an expansion, and no generator revision identifier is retained yet. |
| 11 | Minimal Godot viewer | Not started. `Main.tscn` holds the exported-build smoke entry point plus a materials/environment preview: a `WorldEnvironment` on the airless starfield sky, a directional light, one sphere per surface material, a shader-displaced ground patch, and overview/surface cameras. These are hand-authored placeholders, not loaded exact primitive revisions from the M0a pipeline. Under decision 0031 the content-gate viewer must resolve data-only geometry, material, texture, collision, and parameter primitives through the Godot adapter and must not use engine resources as the authoritative definition. |
| 12 | Native packaged smoke tests | **Done.** `smoke.sh` and `smoke.ps1` run as CI steps on `ubuntu-latest` and `windows-latest` ([workflow](../.github/workflows/ci.yml)), which download and cache the official Godot 4.7.2 mono editor and export templates per OS before exporting and running the smoke check. Both legs exported, packaged, and printed `SMOKE OK` in continuous integration for `47929a7` on 2026-09-07. |
| 13 | Tool versions pinned | **Done.** .NET SDK 10.0.400 in `global.json`, `net10.0` throughout, Godot.NET.Sdk 4.7.2, central package versions ([decision 0016](decisions/0016-platform-confirmed-and-toolchain-pinned.md)). |
| 14 | Reference machines recorded | **Partial.** The Linux workstation is recorded in decision 0016; no Windows reference machine is identified. |
| 15 | Define the core-owned transport contract, commands, and join handshake, tested over an in-memory transport in continuous integration | **Partial.** `ITransport`, `InMemoryTransport`, `CommandJournal`, and the canonically encoded `JoinRequest`/`JoinResponse`/`JoinHandshake` exist under `src/SpaceExplorer.Core/Networking/` ([decision 0028](decisions/0028-in-memory-transport-command-and-handshake-contract.md)). Mutation-checked on two rules: the connect wiring delivering the wrong peer identity, and a replayed command ID becoming acceptable again, each of which turns the suite red. Not held: no real command or campaign exists to run over this yet, and the Godot/ENet adapter is out of scope until M1/M2. |
| 16 | Content gate: reviewer sort against a pre-recorded threshold, and the silhouette comparison finds no near-duplicates | Not started. Blocked on criteria 1 and 11 ([decision 0002](decisions/0002-primitive-set-content-gate.md)). |

Decision 0031 widened the plan's M0a row after this table was numbered, and decisions 0022, 0029, and 0030 cite the numbers, so the rows keep their numbers and the added items are tracked inside them: the definition of exact revisions, the category/parameter/connector contract, storage policies, and canonical set records under criterion 1; data-only package bounds and material/texture/mesh dependency hashes under criterion 6; revision boundaries and collisions under criterion 7.

Immediate implementation steps, in the order [decision 0047](decisions/0047-usable-storage-and-structure-generation-then-solar-system-iterations.md) fixes. First, the primitive storage and the generation of structures from stored sets reach a usable state: the two-process determinism comparison over `generate-set`, which closes a Determinism clause; the loader accepting every registry revision this build supports, as [decision 0035](decisions/0035-category-registry-record-and-generator-revision-identifiers.md) requires, tested for criterion 10 by loading a pack published under revision 1 alongside a revision 2; and a composition generator that builds a bounded ordered graph of instances from a stored set under a versioned grammar, with instance paths, connector-typed transforms, per-instance streams, and connector-tag compatibility enforced, published and reloaded through the set protocol and frozen by a recorded fixed-seed graph hash on both operating systems. Second, the solar-system iterations of M0b: the describe command of [decision 0045](decisions/0045-close-views-only-in-the-surface-sky-and-a-derived-system-description.md) first, because it is the criterion, then numbered iterations of the system grammar and vocabulary recorded under [Solar-system iterations](#solar-system-iterations). Third, the content-gate test plan that [decision 0002](decisions/0002-primitive-set-content-gate.md) presupposes, the minimal Godot viewer of criterion 11, and the content gate of criterion 16. The authored allocation ledger of criterion 2 and the `uint.MaxValue` and overflow tests of criterion 7 fit anywhere as filler; the Windows reference machine of criterion 14 stays open item 3; benchmark thresholds remain for M0b. P0 sessions with three to five real players and their exit record remain required in parallel, but no longer block this work ([decision 0030](decisions/0030-m0a-criterion-1-proceeds-before-the-p0-exit-record.md)).

## Solar-system iterations

The generation iterations the plan's M0b row names ([decision 0047](decisions/0047-usable-storage-and-structure-generation-then-solar-system-iterations.md)). Each row pins what reproduces the iteration and records the verdict on its descriptions; an iteration passes when the validator accepts every description against its tier's rules of [decision 0044](decisions/0044-tiered-system-composition-one-star-and-explorability-by-validation.md) and the owner judges the sample plausible and varied.

| # | Registry revision, vocabulary hash, grammar version, seed list | What changed | Verdict | Commit |
| --- | --- | --- | --- | --- |
| none | No iteration has run. The describe command does not exist and no composition generator publishes a graph; the first iteration begins when step one of decision 0047 closes. | | | |

## Verification and performance targets

The plan's twelve checks, under the plan's names.

| Check | Status |
| --- | --- |
| Native platform support | **Partial.** `dotnet build` and `dotnet test` run on `ubuntu-latest` and `windows-latest` in continuous integration for every push and pull request, with zero build warnings under warnings-as-errors; the suite is 179 tests, 161 in Core, 15 in Persistence, and 3 in `SpaceExplorer.Cli.Tests`, which launches the command-line tool as a child process. The run for `20c18f7` passed on Linux and failed on Windows: every Core test passed, and 7 Persistence tests failed only in cleanup, because Microsoft.Data.Sqlite connection pooling kept `index.db` open after `Dispose` and Windows refuses to delete an open file where Linux allows it. Pooling is disabled for the index, and the re-run for `b7e2df9` passed on both operating systems on 2026-09-07: 161 Core and 11 Persistence tests each. Native SQLite loads on both. The packaged game now exports and launches headlessly on a clean CI runner on both operating systems, printing `SMOKE OK` (M0a exit criterion 12). Not held: this smoke check exercises no graphics or input, only that the exported build boots and reports its runtime versions, so graphics and input remain unverified on Windows; there is also no save format to exchange and no co-op test. |
| Primitive-set foundation | **Partial.** The same specification generates the same manifest hash, and a recorded vector freezes it, on the Debian 13 workstation; the recorded manifest hash also held on `windows-latest` for `20c18f7`, so the same specification yields the same set on both operating systems ([decision 0042](decisions/0042-category-registry-revision-1-first-content-records-generator-and-publish-protocol.md)); worker counts are untested. Saved definitions reload without generation; corrupt, missing, and unsupported-registry cases fail explicitly. Tested: zero and reserved IDs, duplicate content, a second manifest under one `pack_id`. Not tested: values above the signed-32 range, `uint.MaxValue`, overflow, mixed-pack lookup, and the packed-handle byte measurement. |
| Determinism | **Partial.** Two of the plan's clauses hold. The banned-API analyzer gates the core build: `RS0030` was re-checked on a `System.Random` use compiled into Core after the first real code landed. The random primitives produce bit-identical output on both operating systems, evidenced by the random-foundation tests among the 69 Core tests now passing on `ubuntu-latest` and `windows-latest`, including three derivation vectors whose digests were computed outside this repository and the surviving external anchors, the PCG32 reference demo for seed 42 and stream 54 and the two published SHA-256 vectors. Since [decision 0021](decisions/0021-sha256-stream-derivation.md) the derivation is SHA-256 over a canonical record, so the core freezes one published hash rather than a construction of its own. The suite is mutation-checked, so it is load-bearing: perturbing the LCG multiplier, swapping the derived halves, reading the digest big-endian, changing the domain label, and dropping the shift that forces the increment odd each turn it red. Canonical encoding is no longer missing from this check: identity-bearing bytes have a frozen format and a specified hash ([decision 0020](decisions/0020-canonical-encoding-and-content-hash.md)) whose recorded vector and derived pack identifier come out identical on `ubuntu-latest` and `windows-latest`, and the writer admits no floating-point value, no host byte order, and no text outside the ASCII set that path segments use. The first stored records exist, the registry, vocabulary, specification, manifest, and definitions ([decision 0042](decisions/0042-category-registry-revision-1-first-content-records-generator-and-publish-protocol.md)), and one specification reproduces one manifest hash locally. The two-process clause now holds: `SpaceExplorer.Cli.Tests` runs `generate-set` twice as separate operating-system processes into separate data roots and compares the specification hash, pack identifier, manifest hash, and every published record byte for byte, then validates the pack from a third process. Not held: no placements or prices to compare, and no thread-count or interrupted-run coverage. |
| Surface-sky consistency | **Not started.** The current `Main.tscn` materials/environment preview has a fixed directional light and a sky shader with a matching star disc, but its procedural stars are decorative and its two other planets are fixed coloured discs explicitly marked as non-simulated. There is no system epoch, saved astronomical time, orbital propagation, planet rotation, observer-to-planet mapping, phase, horizon/occlusion solution, or atmospheric path beyond the airless placeholder ([decision 0032](decisions/0032-astronomically-consistent-surface-sky.md)). |
| Destination validity | **Not started.** No generator, so no seed suite. The rules it will check are per tier since [decision 0044](decisions/0044-tiered-system-composition-one-star-and-explorability-by-validation.md), and it gains a reviewed sample of system descriptions under [decision 0045](decisions/0045-close-views-only-in-the-surface-sky-and-a-derived-system-description.md). |
| Compatibility | **Not started.** No world, save, or revised template pack exists to revisit. The loader half of M0a exit criterion 10 holds: two registry revisions load side by side from one data root and an unsupported revision fails by name. |
| Durability | **Not started.** No transactions, ledger, or save format exists. |
| Multiplayer | **Not started.** The in-memory transport, command journal, and join handshake exist as of M0a exit criterion 15 ([decision 0028](decisions/0028-in-memory-transport-command-and-handshake-contract.md)), but no real command, campaign, or hazard simulation exists to test over them yet, so none of this check's actual cases (separate connections, late join, simultaneous pickup, permissions, host-side hazard authority, and the rest) can run. |
| Responsiveness | **Not started.** |
| Resource use | **Not started.** [Decision 0017](decisions/0017-region-extent-cap-and-storage-derivation.md) derives a 3 MiB raw maximal height/biome fixture and a 1 MiB compressed target. Under decision 0031 this is one materialized/hybrid terrain candidate, while M0b must also measure cold regeneration, warm-cache load, authoritative bytes, cache bytes, and whether each category stays ahead of exploration. |
| Artifact variety | **Not started.** |
| Gameplay/economy | **Not started.** |

The PCG32 anchor under Determinism was reproduced from an implementation written against the published algorithm rather than read from the source on the day, so it is corroboration and remains worth confirming against pcg-random.org. The two SHA-256 anchors are the published examples for the empty input and for `abc`, and the suite checks them against .NET's SHA-256; the derivation-vector digests were produced by a second implementation, so no vector rests on this repository's code alone ([decision 0021](decisions/0021-sha256-stream-derivation.md)).

## Decisions before production commitments

The plan's seven rows, with what has changed since it was written.

| Decision | Status |
| --- | --- |
| Long-wait role | Settled for the core release by [decision 0004](decisions/0004-preparation-is-real-generation-time.md). The M3 question, whether very large budgets produce content players value, is untested. |
| Pricing and progression | Open. Needs the simulation of repeated expeditions the plan describes. |
| Stack validation, OS versions, hardware | **Partial.** The stack is confirmed and pinned ([decision 0016](decisions/0016-platform-confirmed-and-toolchain-pinned.md)) and library builds and tests pass on both operating systems. Unvalidated: native packaging on Windows, one generated region, a saved artifact, the networking proof at M2 start, and the initial x86_64 and Compatibility scope. Supported distributions, Windows versions, graphics requirements, and reference machines are not confirmed, so no minimum requirements can be published. |
| Numeric content and suit limits | **Partial.** Units, position encoding, heightfield resolution, and tick are fixed by [decision 0010](decisions/0010-units-coordinates-and-region-bounds.md) and the region cap by [decision 0017](decisions/0017-region-extent-cap-and-storage-derivation.md). Generation and storage costs are unmeasured, so planet and region counts per tier, artifact counts, size classes, material rarity weights, effort scores, prices, suit envelopes, and site difficulty are untuned, and no versioned numeric tables exist yet ([decision 0044](decisions/0044-tiered-system-composition-one-star-and-explorability-by-validation.md), [decision 0046](decisions/0046-artifact-families-human-height-cap-cargo-by-mass-and-material-rarity.md)). |
| Camera and art direction | **Partial design.** Presentation remains open for M1 feedback, but decision 0032 fixes the astronomical relationships that art direction may not override: celestial position, phase, angular size, visibility, and the agreement between star direction and lighting. |
| Internet hosting | Open by design; the connectivity approach is decided at M1 start, self-hosted relay and coordination server evaluated first ([decision 0003](decisions/0003-network-topology-and-transport.md)). No service is selected. |
| Schedule and budget | Open. Awaits the owner's team capacity and budget; no milestone estimates exist. |

## Open items outside the plan's process

Carried from [decision 0016](decisions/0016-platform-confirmed-and-toolchain-pinned.md) and later records. These are not rows in the development plan, and are listed here so they are not lost.

| Item | State |
| --- | --- |
| Chunked mesh with level of detail | **Partial design.** Decision 0031 requires deterministic independently generated chunks, stable cross-chunk paths, and evictable level-of-detail caches. The concrete chunk dimensions, mesh strategy, and Godot implementation remain open for the M0b measurement; Godot supplies no terrain or LOD system. Decision 0017 leaves a maximal region at about a million cells (open item 1). |
| P0 built on the Godot stack | Decided by [decision 0022](decisions/0022-p0-gates-the-first-frozen-content-format.md): P0 uses the pinned Godot 4.7.2 .NET stack; its location relative to the solution is settled by the owner's P0 assessment (open item 2). |
| Native Windows machine or runner | Open (open item 3). The `windows-latest` job builds and tests the libraries but never launches the exported game, so requirement R2 is not demonstrated ([requirements](requirements.md)). |
| Exported-build smoke tests in continuous integration | **Closed.** The [workflow](../.github/workflows/ci.yml) downloads and caches Godot 4.7.2 mono and its export templates from the official release per OS, then runs `smoke.sh`/`smoke.ps1`; both passed for `47929a7`. See M0a exit criterion 12. |
| Licence | Deferred. All rights reserved until decided ([decision 0014](decisions/0014-tooling-and-licence.md)). |

## Design documentation

Complete and self-consistent as of 2026-09-08: the nineteen documentation-stage [review](review.md) findings are cleared, forty-seven [decision records](decisions/README.md) are accepted, and the documentation checker reports 66 documents, 1364 internal links, and zero problems. The implementation review of the same day raised [findings 20 to 27](review.md#findings-raised-by-the-implementation-review), all cleared: one by a technical-design change and seven by decisions 0021 to 0024; decision 0025 settles the P0 assessment decision 0022 reserved. A scan of the development plan raised [findings 28 and 29](review.md#findings-raised-by-the-development-plan-scan), cleared by decisions 0026 and 0027; decision 0028 specifies the transport, command, and handshake contract decision 0027 scheduled. Decision 0029's revision-blind identity/handle contract is superseded by decision 0031 before package code consumes it; decision 0030 lets that work proceed before the P0 exit record. The surface-view review raised [finding 30](review.md#30-surface-sky-is-decorative-rather-than-astronomically-derived), cleared by decision 0032. An inspection of the primitive storage and handling plan raised [findings 31 to 36](review.md#findings-raised-by-the-primitive-storage-inspection), and a check of the same structures against the realistic planetary model of decision 0032 raised [findings 37 to 41](review.md#findings-raised-by-the-planetary-model-sufficiency-check); all eleven were cleared the same day on the recommended path: [decision 0033](decisions/0033-generated-definitions-have-one-revision.md) clears 31, [0034](decisions/0034-storage-policy-pinned-by-manifests.md) clears 32, [0035](decisions/0035-category-registry-record-and-generator-revision-identifiers.md) clears 33 and 34, [0036](decisions/0036-frames-and-transforms-typed-by-connector-kind.md) clears 37, [0037](decisions/0037-derivation-rules-integer-periods-and-orbit-hierarchy.md) clears 40, [0038](decisions/0038-derived-instances.md) clears 41, [0039](decisions/0039-one-manifest-per-pack-identifier.md) clears 35, [0040](decisions/0040-reference-index-and-deletion-sweep.md) clears 36, and [decision 0041](decisions/0041-planet-fields-tangent-regions-minimum-radius-and-far-field.md) clears 38 and 39. The owner's generation note of 2026-09-08 raised [finding 42](review.md#42-the-generation-note-diverges-from-the-concept-plan-and-accepted-decisions), cleared the same day by decisions [0043](decisions/0043-destination-controls-distance-tier-and-seed-lever.md) to [0046](decisions/0046-artifact-families-human-height-cap-cargo-by-mass-and-material-rarity.md), which refine concept 2, 3, and 6, add requirements R13 and R14, and retire the note itself. [decision 0047](decisions/0047-usable-storage-and-structure-generation-then-solar-system-iterations.md) orders the next steps: usable storage and structure generation, then the solar-system iterations judged by their descriptions, then the viewer and content gate. Apart from the random foundation, the canonical encoding, and the registry, set, and publication contracts of decision 0042, the design statements remain untested against code.
